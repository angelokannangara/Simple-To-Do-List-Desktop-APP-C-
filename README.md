# 📝 Simple To-Do List (C# WinForms)

A lightweight, desktop-based To-Do List application built with **C#** and **Windows Forms**. This project demonstrates basic CRUD operations and persistent data storage using local file handling.

---

## ✨ Features

* **Add Tasks:** Quickly type and add tasks to your list.
* **Remove Tasks:** Select and delete completed or unwanted tasks.
* **Persistent Storage:** Tasks are automatically saved to a `tasks.txt` file, so your data remains even after closing the app.
* **Input Validation:** Prevents empty or whitespace-only tasks from being added.

---

## 🚀 How to Run

1.  **Clone the Repository:**
    ```bash
    git clone [https://github.com/YourUsername/TodoApp.git](https://github.com/YourUsername/TodoApp.git)
    ```
2.  Open the `.sln` file in **Visual Studio**.
3.  Press `F5` or click **Start** to run the application.

---

## 🛠️ Tech Stack

* **Language:** C#
* **Framework:** .NET / Windows Forms
* **Storage:** System.IO (Text File Persistence)

---

## 🧠 What I Learned

Through this project, I practiced:
* **Event-Driven Programming:** Handling button clicks and UI updates.
* **Collections:** Managing data using `ListBox` items and `List<string>`.
* **File I/O:** Reading from and writing to the local file system.
